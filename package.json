{
  "name": "b-proxy",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "start": "NODE_ENV=production forever start -a --uid proxy -e error.log -o server.log server.js",
    "stop": "forever stop proxy"
  },
  "dependencies": {
    "http-proxy": "^1.11.1"
  }
}
